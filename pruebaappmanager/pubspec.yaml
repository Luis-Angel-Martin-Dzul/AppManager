name: pruebaappmanager
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  grouped_list: ^3.7.1
  flutter:
    sdk: flutter
  google_fonts: ^1.0.0

  cupertino_icons: ^1.0.0
  http: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  
  assets:
    - images/
